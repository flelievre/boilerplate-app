import lightTheme from './lightTheme';
import darkTheme from './darkTheme';

const themesByNames = {
  light: lightTheme,
  dark: darkTheme,
};

export {
  lightTheme,
  darkTheme,
  themesByNames,
};
